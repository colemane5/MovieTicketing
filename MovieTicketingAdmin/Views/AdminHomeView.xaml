<UserControl x:Class="MovieTicketingAdmin.Views.AdminHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieTicketingAdmin.Views"
             xmlns:models="clr-namespace:SharedResources.Models;assembly=SharedResources" d:DataContext="{d:DesignInstance Type=models:User}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="50" />
            <Setter Property="FontSize" Value="30" />
            <Setter Property="Margin" Value="0 8" />
            <Setter Property="Padding" Value="10 0" />
        </Style>
    </UserControl.Resources>
    
    <DockPanel Margin="30">
        <TextBlock Text="{Binding Name, StringFormat={}Hello\, {0}}" DockPanel.Dock="Top" FontSize="50" />
        <Button Content="View Stats" DockPanel.Dock="Top" HorizontalAlignment="Left" />
        <Button Content="Modify Tables" DockPanel.Dock="Top" HorizontalAlignment="Left" />
        <Button Content="Logout" DockPanel.Dock="Bottom" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0"/>
    </DockPanel>
    
</UserControl>
